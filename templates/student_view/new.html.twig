{% extends 'base.html.twig' %}

{% block title %}New StudentView{% endblock %}

{% block body %}
    <h1>Create new StudentView</h1>

    {{ include('student_view/_form.html.twig') }}

    <a class="btn btn-secondary" href="{{ path('student_view_index') }}">back to list</a>
{% endblock %}
